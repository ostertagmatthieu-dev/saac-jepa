% Figure 1 — SAAC-JEPA architecture (Meta / I-JEPA visual language)
\documentclass[tikz,border=6pt]{standalone}
\input{meta_style}
\begin{document}
\begin{tikzpicture}[node distance=9mm]

% ---------------- inputs: sensor grid (some channels masked) ----------------
\begin{scope}[shift={(0,0)}]
  \foreach \t [count=\i from 0] in {0,...,7}{
    \foreach \c [count=\j from 0] in {0,...,4}{
      \pgfmathparse{(\j==1 || \j==3) ? 1 : 0}
      \ifnum\pgfmathresult>0
        \node[cellx] at (0.36*\i, -0.36*\j) {};
      \else
        \node[cellm] at (0.36*\i, -0.36*\j) {};
      \fi
    }
  }
  \node[lbl, anchor=north] at (1.26,-1.85) {capteurs pass\'es $x_{1:T}$};
  \node[sub, anchor=north] at (1.26,-2.22) {17 canaux $\cdot$ masquage canal};
\end{scope}

% actions
\node[act, minimum width=26mm] (pact) at (1.26,-3.3) {actions pass\'ees $a_{1:T}$};

% ---------------- context encoder -------------------------------------------
\node[enc, minimum width=24mm, minimum height=16mm] (ctx) at (5.6,-1.2)
  {encodeur\\contexte $f_\theta$};
\node[sub, below=0.5mm of ctx] {tokens capteurs flexibles};
\node[nrm] (n1) at (3.45,-1.2) {RevIN\\(variante)};

% latent
\node[lat] (z) at (8.6,-1.2) {$z_{1:T}$};

% ---------------- predictor ---------------------------------------------------
\node[prd, minimum width=24mm, minimum height=14mm] (pred) at (11.6,-0.3)
  {pr\'edicteur\\causal $g_\phi$};
\node[act, minimum width=24mm] (fact) at (11.6,-2.6) {actions futures $a_{T:T+h}$};

% predicted latents
\node[lat] (zhat) at (14.7,-0.3) {$\hat z_{T+h}$};

% ---------------- target branch (EMA) ----------------------------------------
\begin{scope}[shift={(0,-5.4)}]
  \foreach \t [count=\i from 0] in {0,...,3}{
    \foreach \c [count=\j from 0] in {0,...,4}{
      \node[cellm, fill=mpink!55] at (0.36*\i, -0.36*\j) {};
    }
  }
  \node[lbl, anchor=north] at (0.54,-1.85) {\'etats futurs $x_{T+h}$};
\end{scope}

\node[tgt, minimum width=24mm, minimum height=14mm] (te) at (5.6,-6.1)
  {encodeur cible\\$f_{\bar\theta}$ (EMA)};
\node[nrm] (n2) at (3.45,-6.1) {RevIN\\(variante)};
\node[lat] (zt) at (8.6,-6.1) {$z_{T+h}$};

% ---------------- loss --------------------------------------------------------
\node[los] (loss) at (14.7,-3.6) {$\mathcal{L}$};
\node[sub, right=1mm of loss, align=left]
  {latente + VICReg\\ + sch\'ema + actions};
\node[sub, text=mnormL, align=left, below=6mm of loss, anchor=north]
  {pointill\'e vert : variante RevIN post-lock (App.~H) ---\\
   stats de la fen\^etre contexte sur les deux entr\'ees,\\
   inverse sur la t\^ete physique ; absente du mod\`ele verrouill\'e};

% ---------------- arrows ------------------------------------------------------
\draw[ar] (2.75,-0.9) -- (n1.west) node[midway, above, sub] {};
\draw[ar] (n1) -- (ctx.west |- ctx.center);
\draw[ar] (pact.east) -- ($(ctx.west)+(0,-0.55)$);
\draw[ar] (ctx) -- (z);
\draw[ar] (z) -- ($(pred.west)+(0,-0.15)$);
\draw[ar] (fact.north) -- ($(pred.south)+(0,0)$);
\draw[ar] (pred) -- (zhat);
\draw[ar] (1.6,-6.1) -- (n2.west);
\draw[ar] (n2) -- (te);
\draw[ar] (te) -- (zt);
\draw[ar] (zhat) -- (loss);
\draw[ar] (zt.east) -| (loss.south);
\sgmark{12.4,-6.1}

% EMA dashed link
\draw[ema] (ctx.south) to[out=-90,in=90] node[midway, right=1mm, sub, text=mpinkL]
  {EMA $\bar\theta \leftarrow m\bar\theta + (1{-}m)\theta$} (te.north);

\end{tikzpicture}
\end{document}
