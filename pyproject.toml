[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
[project]
name = "cnc-jepa-reviewer-v2"
version = "0.1.0"
dependencies = []
[tool.setuptools.packages.find]
where = ["src"]
